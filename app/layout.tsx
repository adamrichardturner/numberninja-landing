import type { Metadata } from "next"
import { Inter } from "next/font/google"
import "./globals.css"

const inter = Inter({ subsets: ["latin"] })

export const metadata: Metadata = {
  title: "Number Ninja Math Game | Master Math like a Ninja!",
  description:
    "Number Ninja Math Game is available on the iOS App Store and Android Play Store. Challenge your mental arithmetic skills and track your progress.",
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang='en'>
      <body className={inter.className}>{children}</body>
    </html>
  )
}
